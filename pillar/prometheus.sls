# No overrides needed yet — the formula's defaults (self-scrape only) are
# fine to start. Add scrape_configs here once there's something to scrape;
# see formulas/prometheus/pillar.example.sls for the schema.
{}
