# Managed by salt-master/init.sls (previously set once via cloud-init,
# now the source of truth).
# Auto-accept every minion key for now (closed homelab LAN).
# Revisit once the network isn't fully trusted.
auto_accept: true
