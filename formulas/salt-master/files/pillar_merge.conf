# Managed by formulas/salt-master. Required for formulas/users: without
# this, pillar list keys (users:accounts) get overwritten per included
# pillar sls instead of merged, so only the last-included formula's
# accounts would survive.
pillar_merge_lists: True
